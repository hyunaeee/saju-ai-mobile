/* =====================================================================
 * config.js — 프론트엔드 실행 모드 설정
 *
 *  apiBase 가 비어 있으면 → "데모 모드"
 *    · 결제/콘텐츠/질문이 전부 브라우저에서 처리됩니다 (로컬 미리보기용).
 *
 *  apiBase + tossClientKey 를 채우면 → "실서비스(보안) 모드"
 *    · 결제는 토스페이먼츠 결제창으로, 승인 검증은 서버(/api/confirm)에서,
 *      심층 리포트 본문은 서버(/api/report)에서, 질문 답변은 서버(/api/ask,
 *      claude-haiku-4-5)에서 처리됩니다.
 *
 *  배포 시 이 파일의 두 값만 바꾸면 됩니다. (API 시크릿 키는 절대 여기 넣지 마세요)
 * ===================================================================== */
window.CHEONGIYEON_CONFIG = {
  // 예: "https://your-app.vercel.app"  (같은 도메인이면 "" 로 두고 상대경로 사용)
  apiBase: "",

  // 토스페이먼츠 "클라이언트 키"(test_ck_... / live_ck_...) — 공개되어도 되는 키입니다.
  // 시크릿 키(test_sk_.../live_sk_...)는 서버 환경변수에만 넣으세요.
  tossClientKey: "",
};
